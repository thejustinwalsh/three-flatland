# Capture artifacts — generated by `pnpm --filter=docs capture:examples`.
# Stills + videos for the gallery masonry are runtime-regenerable from
# the examples, so we don't bloat the git history with binary content.
# The committed `.gitkeep` keeps the directory present for the build.
public/captures/*
!public/captures/.gitkeep
