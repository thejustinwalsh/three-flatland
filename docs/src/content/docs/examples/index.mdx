---
title: Examples
description: Focused single-feature demos of three-flatland — sprites, animation, batch rendering, TSL nodes, pass effects, tilemaps, and Skia integration.
template: splash
---

import GalleryGrid from '../../../components/gallery/GalleryGrid.astro'
import GalleryTile from '../../../components/gallery/GalleryTile.astro'

<div class="examples-page not-content">

<header class="examples-header">
  <p class="examples-eyebrow">Examples</p>
  <h1 class="examples-title">Shortcuts to getting started.</h1>
  <p class="examples-tagline">Self contained cloneable templates that demonstrate real world use of three-flatland features.</p>
</header>

<GalleryGrid>
  <GalleryTile
    surface="examples"
    slug="basic-sprite"
    title="Basic Sprite"
    description="Minimal Sprite2D setup with hover and press states."
  />
  <GalleryTile
    surface="examples"
    slug="animation"
    title="Animation"
    description="CPU-clocked spritesheet animation, frames sampled from a packed atlas."
  />
  <GalleryTile
    surface="examples"
    slug="batch-demo"
    title="Batch Rendering"
    description="Interactive building placement, one shared material, batchCount locked at 1."
  />
  <GalleryTile
    surface="examples"
    slug="knightmark"
    title="Knightmark"
    description="Spatial-hashed knight collision stress test, spacebar adds 100 more."
  />
  <GalleryTile
    surface="examples"
    slug="pass-effects"
    title="Pass Effects"
    description="Full-screen post-processing: CRT, LCD, VHS, scanlines, swapped between presets."
  />
  <GalleryTile
    surface="examples"
    slug="tsl-nodes"
    title="TSL Nodes"
    description="Per-sprite TSL node shader effects, still drawn in one shared batch."
  />
  <GalleryTile
    surface="examples"
    slug="tilemap"
    title="Tilemap"
    description="A procedurally generated dungeon as batched tile layers, with animated tiles."
  />
  <GalleryTile
    surface="examples"
    slug="skia"
    title="Skia"
    description="Skia WASM vector rendering through the active WebGPU or WebGL backend."
  />
  <GalleryTile
    surface="examples"
    slug="slug-text"
    title="Slug Text"
    description="Per-pixel Bezier glyph outlines via the Slug algorithm, crisp at any scale."
  />
  <GalleryTile
    surface="examples"
    slug="lighting"
    title="2D Lighting"
    description="Forward+ lighting (DefaultLightEffect) in a dungeon: torch flicker, click-to-walk."
  />
  <GalleryTile
    surface="examples"
    slug="hit-test"
    title="Hit Testing"
    description="Raycaster sprite picking: hover, drag, and walk-to-collect coins."
  />
</GalleryGrid>

</div>

<style>
{`
  /* MDX-safe — CSS wrapped in a template literal because MDX 3
     treats { and } in raw <style> content as JS expression
     boundaries. Same pattern the landing page (index.mdx) uses. */
  .examples-page {
    max-width: 75rem;
    margin: 0 auto;
    padding-inline: clamp(1rem, 4vw, 2rem);
    padding-block: clamp(2rem, 4vw, 3rem);
  }

  .examples-header {
    margin-block-end: clamp(2rem, 4vw, 3rem);
    max-width: 56rem;
  }

  .examples-eyebrow {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 0.75rem;
    font-weight: 600;
    color: color-mix(in oklab, var(--diamond) 60%, var(--muted-foreground));
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin: 0 0 0.5rem 0;
  }

  .examples-title {
    font-family: 'Public Sans', system-ui, sans-serif;
    font-size: clamp(2rem, 4.5vw, 3.25rem);
    font-weight: 200;
    letter-spacing: -0.025em;
    line-height: 1.05;
    color: var(--foreground);
    margin: 0 0 1rem 0;
    text-wrap: balance;
  }

  .examples-tagline {
    font-family: 'Public Sans', system-ui, sans-serif;
    font-size: clamp(1rem, 1.5vw, 1.0625rem);
    font-weight: 400;
    line-height: 1.55;
    color: var(--muted-foreground);
    margin: 0;
    max-width: 48rem;
    text-wrap: balance;
  }
`}
</style>
