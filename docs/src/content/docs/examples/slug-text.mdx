---
title: Slug Text
description: GPU-accelerated resolution-independent text rendering.
---

import { Tabs, TabItem } from '@astrojs/starlight/components'
import ExamplePreview from '../../../components/ExamplePreview'
import StackBlitzEmbed from '../../../components/StackBlitzEmbed'
import ExampleTabs from '../../../components/ExampleTabs.astro'
import { loadExample } from '../../../utils/loadExample'

export const threeFiles = loadExample('three', 'slug-text')
export const reactFiles = loadExample('react', 'slug-text')

<ExampleTabs>
  <Tabs>
    <TabItem label="Preview" icon="rocket">
      <ExamplePreview client:load type="three" name="slug-text" />
    </TabItem>
    <TabItem label="Three.js" icon="seti:javascript">
      <StackBlitzEmbed
        client:load
        title="Slug Text - Three.js"
        files={threeFiles}
        openFile="main.ts"
        height={600}
      />
    </TabItem>
    <TabItem label="React" icon="seti:react">
      <StackBlitzEmbed
        client:load
        title="Slug Text - React"
        files={reactFiles}
        openFile="App.tsx"
        height={600}
      />
    </TabItem>
  </Tabs>
</ExampleTabs>

## SlugFontLoader

`SlugFontLoader.load()` is the single entry point. It tries baked data (`.slug.json` + `.slug.bin`) first and falls back to runtime parsing of TTF/OTF/WOFF — opentype.js is dynamic-imported only on the runtime path:

```typescript
import { SlugFontLoader } from '@three-flatland/slug'

const font = await SlugFontLoader.load('/fonts/Inter-Regular.ttf')
```

## SlugText

`SlugText` extends `InstancedMesh` — each glyph is an instanced quad rendered per-pixel from Bezier curves. No texture atlases, no SDF, resolution-independent at any zoom:

```typescript
const text = new SlugText({
  font,
  text: 'Hello, Slug!',
  fontSize: 48,
  color: 0xffffff,
  align: 'center',
})
scene.add(text)
```

Call `text.update(camera)` every frame to keep the MVP matrix current for dilation.

## Next Steps

- [Slug Text Guide](../guides/slug-text) - Full API reference and patterns
- [Sprites](../examples/basic-sprite) - 2D sprite rendering
