---
title: Installation
description: Install three-flatland in your project.
---

import { Tabs, TabItem, Aside } from '@astrojs/starlight/components';

<Aside type="caution" title="Coming Soon">
  three-flatland is not yet published to npm. We're preparing the initial alpha release. Star the repo to get notified when it's ready!
</Aside>

## Package Manager

<Tabs syncKey="pkg">
  <TabItem label="npm">
    ```bash
    npm install @three-flatland/core@alpha three
    ```
  </TabItem>
  <TabItem label="pnpm">
    ```bash
    pnpm add @three-flatland/core@alpha three
    ```
  </TabItem>
  <TabItem label="yarn">
    ```bash
    yarn add @three-flatland/core@alpha three
    ```
  </TabItem>
  <TabItem label="bun">
    ```bash
    bun add @three-flatland/core@alpha three
    ```
  </TabItem>
</Tabs>

### React Three Fiber

For React projects using React Three Fiber:

<Tabs syncKey="pkg">
  <TabItem label="npm">
    ```bash
    npm install @three-flatland/react@alpha @react-three/fiber@alpha three react react-dom
    ```
  </TabItem>
  <TabItem label="pnpm">
    ```bash
    pnpm add @three-flatland/react@alpha @react-three/fiber@alpha three react react-dom
    ```
  </TabItem>
  <TabItem label="yarn">
    ```bash
    yarn add @three-flatland/react@alpha @react-three/fiber@alpha three react react-dom
    ```
  </TabItem>
  <TabItem label="bun">
    ```bash
    bun add @three-flatland/react@alpha @react-three/fiber@alpha three react react-dom
    ```
  </TabItem>
</Tabs>

## Requirements

- **Three.js**: >= 0.182.0 (required for TSL/WebGPU support)
- **React**: >= 19.0.0 (for `@three-flatland/react`)
- **@react-three/fiber**: >= 10.0.0-alpha.0 (for `@three-flatland/react`)
- **Node.js**: >= 22 (LTS)

## TypeScript

three-flatland is written in TypeScript and ships with full type definitions. No additional `@types` packages are required.
