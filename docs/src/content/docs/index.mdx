---
title: three-flatland
description: The complete 2D solution for Three.js and React Three Fiber. Built for WebGPU with composable TSL shaders.
template: splash
---

import FeatureCard from '../../components/FeatureCard.astro';
import StatsBanner from '../../components/StatsBanner.astro';
import ValueProp from '../../components/ValueProp.astro';
import HeroGame from '../../components/HeroGame.tsx';
import { Tabs, TabItem, Aside } from '@astrojs/starlight/components';

<section class="hero-fullscreen not-content">
  <div class="hero-canvas" aria-hidden="true">
    <HeroGame client:visible />
  </div>
  <div class="hero-overlay">
    <div class="hero-overlay-inner">
      <h1 class="hero-title">three-flatland</h1>
      <p class="hero-tagline">
        Sprite-first 2D for Three.js + React Three Fiber. WebGPU-native.
        TSL-composable. Designed to render thousands of things at 60fps without you having to think about it.
      </p>
      <div class="hero-actions">
        <a href="/three-flatland/getting-started/introduction/" class="sl-link-button primary">
          Get Started
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M5 12h14M12 5l7 7-7 7" />
          </svg>
        </a>
        <a href="https://github.com/thejustinwalsh/three-flatland" class="sl-link-button secondary">
          View on GitHub
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M7 17l9.2-9.2M17 17V7H7" />
          </svg>
        </a>
      </div>
    </div>
    <div class="hero-scrollcue" aria-hidden="true">
      <span>Scroll</span>
      <svg viewBox="0 0 16 16" width="14" height="14"><path d="M4 6l4 4 4-4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" fill="none"/></svg>
    </div>
  </div>
</section>

<div class="page-body not-content">

<StatsBanner stats={[
  { value: "10,000+", label: "Sprites at 60fps", color: "diamond" },
  { value: "1", label: "Draw Call", color: "pink" },
  { value: "0", label: "CPU Overhead", color: "gold" },
  { value: "100%", label: "TypeScript", color: "amethyst" },
]} />

<h2 class="section-heading">What ships in the box</h2>

<div class="feature-grid">
  <FeatureCard
    icon="lightbulb"
    title="WebGPU Native"
    color="diamond"
    stat="Next-Gen"
    statLabel="Graphics API"
  >
    Built from the ground up for WebGPU. No WebGL fallbacks, no compromises. Pure TSL shader power for effects that weren't possible before.
  </FeatureCard>

  <FeatureCard
    icon="list"
    title="Batch Rendering"
    color="emerald"
    stat="10,000+"
    statLabel="Sprites per batch"
  >
    Automatic batching and GPU instancing. Every sprite shares geometry and materials. Your GPU does the heavy lifting.
  </FeatureCard>

  <FeatureCard
    icon="user"
    title="React Three Fiber"
    color="amethyst"
    stat="R3F v10"
    statLabel="WebGPU Support"
  >
    First-class R3F integration. Declarative JSX components that feel native. Full TypeScript support with accurate types.
  </FeatureCard>

  <FeatureCard
    icon="play-circle"
    title="Animation System"
    color="gold"
    stat="60fps"
    statLabel="Frame-perfect timing"
  >
    Built-in sprite animation with spritesheet support. Define animations in JSON, play them with one call. Buttery smooth.
  </FeatureCard>

  <FeatureCard
    icon="sparkles"
    title="TSL Shader Nodes"
    color="salmon"
    stat="Composable"
    statLabel="Effect pipeline"
  >
    Mix and match shader effects. Palette swaps, outlines, glow, distortion. Build your visual style node by node.
  </FeatureCard>

  <FeatureCard
    icon="map"
    title="Tilemap Support"
    color="pink"
    stat="Tiled"
    statLabel="JSON format"
  >
    Efficient tilemap rendering with Tiled editor support. Multiple layers, animated tiles, collision data. Build worlds fast.
  </FeatureCard>
</div>

<h2 class="section-heading">Quick Start</h2>

<Tabs>
  <TabItem label="Three.js" icon="seti:javascript">
```typescript
import { Sprite2D, SpriteGroup } from 'three-flatland'

// Create a sprite group and add to your scene
const spriteGroup = new SpriteGroup()
scene.add(spriteGroup)

// Create sprites with textures
const player = new Sprite2D({ texture: playerTexture })
const enemy = new Sprite2D({ texture: enemyTexture })
enemy.position.set(2, 0, 0)

// Add sprites to the group
spriteGroup.add(player, enemy)
```
  </TabItem>
  <TabItem label="React" icon="seti:react">
```tsx
import { Canvas, extend } from '@react-three/fiber/webgpu'
import { Sprite2D, SpriteGroup } from 'three-flatland/react'

extend({ Sprite2D, SpriteGroup })

function Game() {
  return (
    <Canvas>
      <spriteGroup>
        <sprite2D texture={playerTexture} position={[0, 0, 0]} />
        <sprite2D texture={enemyTexture} position={[2, 0, 0]} />
      </spriteGroup>
    </Canvas>
  )
}
```
  </TabItem>
</Tabs>

<h2 class="section-heading">Why three-flatland?</h2>

<ValueProp title="Built for the Future" color="diamond">
  three-flatland is designed **exclusively** for Three.js WebGPU renderer. No legacy code, no WebGL baggage. By targeting WebGPU from day one, we leverage **TSL (Three Shader Language)** for shader composition that simply isn't possible with GLSL strings.

  This means **real-time shader effects** that would tank performance on WebGL run buttery smooth. Palette cycling, CRT effects, per-sprite distortion — all composable, all fast.
</ValueProp>

<ValueProp title="Performance Obsessed" color="emerald" align="right">
  Every sprite in your scene shares the same geometry and materials through **automatic batching**. Whether you have 10 sprites or 10,000, three-flatland keeps your draw calls minimal and your frame rate high.

  The GPU does what GPUs do best. Your CPU stays free for game logic. **Zero per-sprite overhead** in your render loop.
</ValueProp>

<ValueProp title="Developer Joy" color="gold">
  Full **TypeScript** support with types that actually help. React Three Fiber users get JSX components that integrate seamlessly. Three.js users get a clean, intuitive API.

  No configuration hell. No boilerplate. Import, create, render. **Ship your game, not your build config.**
</ValueProp>

</div>

<style>
{`
  /* Fullscreen parallax hero — HeroGame canvas is sticky-pinned to the
     viewport for the duration of the hero section, with title/tagline/
     CTAs overlaid on top. As the user scrolls, the title block slides
     up and away while the canvas stays put for the parallax effect.
     Section is min-height: 100vh + a bit so there's a real "scroll past
     the hero" gesture before the canvas releases. */
  .hero-fullscreen {
    position: relative;
    width: 100%;
    min-height: 100vh;
    margin: 0;
    /* Pull edge-to-edge: splash main has no padding, but other ancestor
       wrappers may add one — neutralize the negative margin if needed. */
  }
  .hero-canvas {
    position: sticky;
    top: 0;
    height: 100vh;
    width: 100%;
    overflow: hidden;
    background: var(--background);
  }
  .hero-canvas > :global(*) {
    position: absolute;
    inset: 0;
  }

  /* Overlay sits ON TOP of the canvas, scrolls naturally with the page,
     centered horizontally and vertically in the visible viewport. */
  .hero-overlay {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: clamp(1.5rem, 4vw, 3rem);
    pointer-events: none;
    z-index: 2;
  }
  /* Bottom-fade gradient masks the canvas to the page background so the
     transition into the .page-body content reads as a soft horizon. */
  .hero-overlay::after {
    content: '';
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;
    height: 33%;
    background: linear-gradient(
      to bottom,
      transparent 0%,
      color-mix(in oklab, var(--background) 70%, transparent) 60%,
      var(--background) 100%
    );
    pointer-events: none;
  }
  .hero-overlay-inner {
    position: relative;
    z-index: 3;
    pointer-events: auto;
    text-align: center;
    max-width: 56rem;
    backdrop-filter: blur(0.5px);
  }

  .hero-title {
    font-family: 'Public Sans', system-ui, sans-serif;
    font-size: clamp(3rem, 7vw, 5.5rem);
    font-weight: 200;
    line-height: 1.02;
    letter-spacing: -0.04em;
    color: var(--foreground);
    margin: 0 0 calc(var(--spacing) * 5);
    text-shadow: 0 2px 32px color-mix(in oklab, var(--background) 75%, transparent);
  }
  .hero-tagline {
    font-family: 'Public Sans', system-ui, sans-serif;
    font-size: clamp(1rem, 1.5vw, 1.125rem);
    font-weight: 400;
    line-height: 1.55;
    color: color-mix(in oklab, var(--foreground) 88%, var(--muted-foreground));
    max-width: 44rem;
    margin: 0 auto calc(var(--spacing) * 8);
    text-shadow: 0 1px 12px color-mix(in oklab, var(--background) 80%, transparent);
  }
  .hero-actions {
    display: flex;
    justify-content: center;
    gap: calc(var(--spacing) * 3);
    flex-wrap: wrap;
  }
  .hero-scrollcue {
    position: absolute;
    bottom: calc(var(--spacing) * 8);
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: calc(var(--spacing) * 1);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 0.6875rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: color-mix(in oklab, var(--muted-foreground) 80%, transparent);
    z-index: 4;
    pointer-events: none;
    animation: scroll-cue-bob 2.4s ease-in-out infinite;
  }
  @keyframes scroll-cue-bob {
    0%, 100% { transform: translateX(-50%) translateY(0); opacity: 0.55; }
    50% { transform: translateX(-50%) translateY(4px); opacity: 0.95; }
  }
  @media (prefers-reduced-motion: reduce) {
    .hero-scrollcue { animation: none; }
  }

  /* Page body — everything below the hero gets reasonable horizontal
     padding so content isn't edge-to-edge on splash pages. The hero
     stays edge-to-edge for the fullscreen vibe. */
  .page-body {
    padding-inline: clamp(1.5rem, 4vw, 3rem);
    padding-block: calc(var(--spacing) * 16) calc(var(--spacing) * 12);
    max-width: 90rem;
    margin: 0 auto;
    position: relative;
    z-index: 1;
    background: var(--background);
  }

  /* Section headings inside .page-body — Public Sans display, smaller
     than h1 and at semi weight for structural clarity, with consistent
     vertical rhythm between sections. */
  .section-heading {
    font-family: 'Public Sans', system-ui, sans-serif;
    font-size: clamp(1.625rem, 2.6vw, 2rem);
    font-weight: 600;
    line-height: 1.15;
    letter-spacing: -0.02em;
    color: var(--foreground);
    margin: calc(var(--spacing) * 16) 0 calc(var(--spacing) * 6);
  }
  .section-heading:first-child {
    margin-top: 0;
  }

  .feature-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(min(100%, 320px), 1fr));
    grid-auto-rows: 1fr;
    gap: 1.5rem;
    margin: 0 0 calc(var(--spacing) * 4);
    align-items: stretch;
  }
  .feature-grid > :global(article) {
    height: 100%;
    display: flex;
    flex-direction: column;
  }
  @media (min-width: 900px) {
    .feature-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }
  @media (min-width: 600px) and (max-width: 899px) {
    .feature-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
`}
</style>
