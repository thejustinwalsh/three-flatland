/** Shared atlas metadata types used by generation and UV helpers. */

export interface Rect {
  x: number
  y: number
  w: number
  h: number
}

export type GemColor = 'emerald' | 'topaz' | 'ruby' | 'amethyst'
export type GemSize = 'small' | 'medium' | 'large' | 'huge'
