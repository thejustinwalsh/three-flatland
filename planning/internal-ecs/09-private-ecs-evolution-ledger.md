# Private ECS evolution ledger

Status: **active reference**

Date opened: 2026-08-24

This ledger records every feature added to the private renderer ECS. The
[architecture standard](./06-private-ecs-architecture-standard.md) defines the invariants, and the
[convergence plan](./07-private-ecs-convergence-plan.md) defines sequencing. This page records what
changed, why it belongs in data-oriented storage, and which evidence accepted it.

## Logging contract

Create or update an entry before feature implementation begins. Each entry records:

- the authoritative owner and data layout,
- the object-owned resources that remain outside numeric storage,
- the public API and declaration-boundary effect,
- the measured performance or ownership hypothesis,
- the required behavioral, allocation, size, and application evidence,
- the final commit, pull request, and accepted result.

Any public documentation produced by an entry must credit
[Koota](https://github.com/pmndrs/koota) as the design foundation for Flatland's private renderer ECS.
The wording must preserve Koota's continuing role as a general-purpose application ECS and must not
frame the internal specialization as an upstream replacement.

Statuses are `active`, `queued`, `evidence-gated`, `accepted`, or `rejected`. A rejected experiment
stays in the ledger with its evidence so the same design is not repeated without new information.

## Feature index

| ID      | Feature                             | Status   | Authoritative state                                                                  | Evidence boundary                                                                               |
| ------- | ----------------------------------- | -------- | ------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------- |
| ECS-001 | Renderer batching kernel            | accepted | Private world traits plus batch-owned packed slot tables                             | PR #232; kernel, renderer, memory, size, package, example, and publication gates                |
| ECS-002 | Construction-time capacity planning | accepted | World and registry capacity reserved from `expectedSprites`                          | PR #233; under/exact/over estimate, reuse, disposal, size, and R3F constructor gates            |
| ECS-003 | Animation playback convergence      | accepted | Object-local controllers plus dense slot-bound shared-timeline cohorts               | Behavioral parity plus allocation and frame-time measurements at 1k, 16k, and 60k sprites       |
| ECS-004 | Render/pass graph consolidation     | accepted | One private graph owner with a persistent ordered projection                         | Atomic graph edits, clean-frame allocation, nested-world lifecycle, and identical TSL output    |
| ECS-005 | Tile-animation compaction           | accepted | Layer-local typed timer/frame arrays and dense dirty-ID projection                   | Catch-up semantics, shared timers, chunk lifecycle, allocation, and large animated-tile timing  |
| ECS-006 | Lighting storage refinement         | accepted | Object-owned lighting lifecycle plus one packed `LightStore` GPU projection          | Low/dense light sync timing, stale-slot cleanup, nested-world disposal, and shader parity       |
| ECS-007 | Hierarchy refinement                | accepted | Three.js hierarchy identity plus a batch-local frame-stamped path projection         | 16,384-sprite Labs transform profile plus hierarchy, visibility, reparent, and allocation gates |
| ECS-008 | Camera-visible instance compaction  | rejected | Object-owned transforms; no per-sprite camera projection added                       | Sparse/dense Labs timing, allocation, sort parity, and existing tile-chunk culling              |
| ECS-009 | Shadow-occluder projection          | active   | Object-owned meshes plus a persistent package-private occluder membership projection | Dirty-pass Labs timing, headed WebGPU output parity, foreign meshes, and resource lifecycle     |
| ECS-010 | Skia retained display list          | queued   | Object3D authoring tree plus a proposed dense package-private command projection     | Benchmark-only PR; full WebGPU redraw, mutation, memory, custom-node, and pixel-parity gates    |

## ECS-003: Animation playback convergence

| Field                  | Record                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Decision               | Reject blanket controller SoA, retain object-local independent state, and accept dense group-owned bindings that reuse one proven-safe transition across synchronized timelines.                                                                                                                                                                                                                                                                                                |
| Numeric state          | Independent elapsed time, current frame, speed, direction, play state, and loop count remain controller-local. Exact entity handles, cohort IDs, group ticks, and controller revisions form a reusable typed binding projection owned by `SpriteGroup`.                                                                                                                                                                                                                         |
| Object state           | `Animation`, `SpriteFrame`, spritesheets, callbacks, event payload definitions, and standalone controller state.                                                                                                                                                                                                                                                                                                                                                                |
| Public boundary        | Keep `AnimatedSprite2D`, its controller methods, and caller-timed `SpriteGroup.advanceAnimations(deltaMs)` as the command surface. No entity, trait, store, selector, or runtime type becomes reachable from package declarations.                                                                                                                                                                                                                                              |
| Projection             | A committed frame transition uses the existing frame/UV update path so packed GPU rows remain the only render projection.                                                                                                                                                                                                                                                                                                                                                       |
| Compatibility          | Standalone sprites continue to support explicit `update(deltaMs)`. Enroll, detach, clone, and spritesheet replacement preserve current semantics.                                                                                                                                                                                                                                                                                                                               |
| Performance hypothesis | Compute one transition for sprites that deliberately share definition, phase, speed, and loop semantics, then commit the same final frame through each existing batch UV row. Full-row controller state remains object-local because columnar relocation was hostile to this access pattern.                                                                                                                                                                                    |
| Required behavior      | Loop, ping-pong, speed, pause/resume, large delta, per-frame duration, events, spritesheet swap, detach/re-enroll, cloning, disposal, and reentrant callbacks.                                                                                                                                                                                                                                                                                                                  |
| Required evidence      | Warm allocation profile and trusted Labs comparison at 1k, 16k, and 60k animated sprites; deterministic GPU-row and event checks; package size and all-example gates.                                                                                                                                                                                                                                                                                                           |
| Public attribution     | Any guide or release note credits Koota's typed-trait and query model as the foundation that made this specialization possible.                                                                                                                                                                                                                                                                                                                                                 |
| Branch                 | `feat/animated-sprite-playback-soa`                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Pull request           | Pending                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Baseline               | Corrected object-backed group baseline `cafafc75`: p50 1k 166.88 µs and 16,384 5.27 ms. Both baseline and candidate use the identical caller-timed `SpriteGroup.advanceAnimations(deltaMs)` fixture; baseline clock drift of 9.1% is retained in the comparison report.                                                                                                                                                                                                         |
| Rejected experiment    | Commit `9c086a86` moved nine playback scalars into enrolled numeric SoA and included a persistent-selector bulk pass, but the fixture still called every sprite's `update(deltaMs)` and did not measure that pass. That direct-update path was slower at 1k (+39.8% p50, `p < .001`) and 16,384 (+64.5%, `p < .001`); 60k was directionally +56% but noisy. The implementation was removed.                                                                                     |
| Result                 | Accepted at `a0257f72`: p50/p99 improved 25.5%/37.9% at 1k and 38.6%/24.5% at 16,384 (`p < .001` for both). The 60k candidate remained directionally faster but noisy under the fixed 30-second convergence ceiling and is not a release claim. Topology-owned membership and pre-sized scratch remove the registry scan and steady-step allocation; direct updates, callbacks, events, custom methods, divergent state, and multi-frame catch-up preserve the controller path. |

## ECS-004: Render/pass graph consolidation

| Field                  | Record                                                                                                                                                                                                                                                                             |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Decision               | Replace overlapping pass arrays, entity scalars, and rebuilt ordered results with one private graph owner.                                                                                                                                                                         |
| Authoritative owner    | A package-private `PostPassGraph` owns insertion membership, insertion order, the dirty revision, and reusable ordered/function projections. `PassEffect` remains authoritative for its enabled state, order, and built TSL function.                                              |
| Object state           | `PassEffect`, `LightEffect`, TSL node functions, materials, render targets, and other GPU resources remain object-owned. Pass membership is object topology, not a numeric ECS query.                                                                                              |
| Numeric state          | Only declared numeric effect fields remain in private trait storage. The previous ECS pass topology trait and registry singleton are removed because they duplicated object state and rebuilt temporary arrays.                                                                    |
| Public boundary        | Existing `Flatland.addPass`, `removePass`, `clearPasses`, `passes`, and `PassEffect.enabled` remain the only control surface. No graph, entity, trait, selector, or store type becomes reachable from package declarations.                                                        |
| Performance hypothesis | Reusing one ordered projection removes query/view/result allocations from dirty rebuilds and makes clean frames a constant dirty check. The change is accepted only if it preserves output and does not increase shipped-runtime or consumer budgets beyond reviewed limits.       |
| Correctness finding    | The previous `PassEffect.enabled` setter dirtied the registry but did not update its duplicated `PostPassTrait.enabled` value, so the rebuilt chain could retain the stale enabled state. Reading the effect's authoritative state during projection removes that split-brain bug. |
| Required evidence      | Atomic add/remove/reorder/enable/lighting replacement, first-error-safe cleanup, nested worlds, zero allocation on clean frames, and identical Three/React TSL output.                                                                                                             |
| Public attribution     | Any public guide or release note credits [Koota](https://github.com/pmndrs/koota)'s typed-trait/query design as the foundation that made Flatland's private specialization possible, while continuing to recommend Koota for general-purpose application ECS work.                 |
| Commit                 | `6e058994` (`perf(pipeline): consolidate post-pass graph ownership`)                                                                                                                                                                                                               |
| Pull request           | Pending                                                                                                                                                                                                                                                                            |
| Evidence               | Full package: 110 files / 1,410 tests with no type errors; type-aware lint; package/declaration/public-boundary build; existing shader-compiler coverage; paired example and docs build; `three-flatland (full)` size 84,303 B Brotli.                                             |
| Result                 | Accepted. Pass topology is object-owned by one graph, declared numeric effect fields remain in private traits, clean projection checks allocate nothing, dirty projections reuse their arrays, and attached `enabled` changes now rebuild from authoritative state.                |

## ECS-005: Tile-animation compaction

| Field                  | Record                                                                                                                                                                                                                                                                                                                                 |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Decision               | Accept the layer-local dense projection. It removes per-frame Map/Set/string work, fixes multi-frame catch-up, and is decisively faster in the frozen production fixture at every measured load.                                                                                                                                       |
| Authoritative owner    | `TileLayer` owns a private `TileAnimationProjection` containing animation IDs, elapsed time, frame indices, chunk membership, and dirty projection. `Tileset` remains authoritative for validated animation definitions and atlas UVs.                                                                                                 |
| Object state           | Tileset definitions, source frame records, chunk meshes, interleaved GPU buffers, and material resources remain object-owned. Packed frame durations and UVs are immutable layer-build projections, not a second public owner.                                                                                                         |
| Numeric state          | Stable animation IDs, elapsed time, frame indices, per-position animation IDs, instance indices, chunk IDs, changed marks, and dirty marks use exact typed arrays sized when the layer projection is sealed.                                                                                                                           |
| Public boundary        | `TileLayer.update(deltaMs)` and `TileMap2D.update(deltaMs)` remain the only command surface. No scheduler, timer, typed array, animation ID, entity, trait, or store becomes public.                                                                                                                                                   |
| Correctness finding    | The previous timer loop advanced at most one frame per call. The accepted scheduler consumes every completed variable-duration frame, uses total-cycle reduction for large deltas, rejects empty/non-positive/non-finite/overflowing definitions at Tileset construction, and rejects non-finite update deltas before mutating clocks. |
| Performance hypothesis | One timer update per unique base GID plus a dense animation-ID projection avoids per-position Map/Set/string lookups and coalesces each dirty chunk upload marker.                                                                                                                                                                     |
| Required behavior      | Variable frame durations, multi-frame catch-up and wrap, shared timers, multiple animations/chunks, zero animated tiles, topology rebuild, setTileAt animated/static transitions, disposal, terminal guards, and exact dirty-buffer publication.                                                                                       |
| Required evidence      | Frozen object-state and candidate Labs comparison at representative 1k, 16,384, and 60k animated tiles; focused lifecycle/UV tests; full package/type/build/lint; size and paired tilemap example gates.                                                                                                                               |
| Public attribution     | Any public guide or release note credits [Koota](https://github.com/pmndrs/koota) as the typed-trait/query foundation that enabled Flatland's private data-oriented specialization, while keeping Koota the recommended general-purpose application ECS.                                                                               |
| Branch                 | `feat/animated-sprite-playback-soa`                                                                                                                                                                                                                                                                                                    |
| Pull request           | Pending                                                                                                                                                                                                                                                                                                                                |
| Baseline               | Frozen fixture commit `314a67b4`; object-state p50: 1k 34.92 µs, 16,384 489.65 µs, 60k 1.75 ms. Baseline clock drift was 9.4%, so the paired Labs statistical verdict—not raw movement alone—controls acceptance.                                                                                                                      |
| Commit                 | `bd0dda07` (`perf(tilemap): compact animated tile playback`)                                                                                                                                                                                                                                                                           |
| Evidence               | Labs candidate p50: 1k 8.17 µs (-76.6%), 16,384 104.25 µs (-78.7%), 60k 370.23 µs (-78.8%); all `p < .001`, all measurements stable, conservative noise bands ±23%/±9%/±10%. Package: 111 files / 1,419 tests, no type errors; type-aware lint; build/declaration boundary; Three/React tilemap builds; full bundle 84.81 kB Brotli.   |
| Result                 | Accepted. One dense clock is updated per shared animation, packed UVs are projected directly into exact tile rows, dirty chunks are marked once, public APIs stay unchanged, and disposal releases every retained chunk reference.                                                                                                     |

## ECS-006: Lighting storage refinement

| Field                  | Record                                                                                                                                                                                                                                                                                                                                                                                |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Decision               | Accept the bounded `LightStore` stale-slot retirement. Do not duplicate `Light2D` fields or `LightingContext` object topology into another numeric ECS trait.                                                                                                                                                                                                                         |
| Authoritative owner    | `Flatland` owns light membership and lighting lifecycle. `Light2D` remains authoritative for public mutable light state. `LightStore` remains the single packed CPU/GPU numeric projection. `ShadowPipeline` remains the single owner of shadow resources and camera stamps.                                                                                                          |
| Object state           | Light/effect instances, renderer, camera, scene, materials, channel sets, TSL functions, textures, render targets, and lifecycle flags remain object-owned.                                                                                                                                                                                                                           |
| Numeric state          | `LightStore` already owns the exact 4-row × 4-channel packed float projection sampled by shaders. Shadow dimensions and camera stamps remain a handful of scalars on the one world-owned pipeline object; a second SoA would add lookups rather than improve iteration.                                                                                                               |
| Public boundary        | `Light2D`, `LightEffect`, `LightStore`, and `Flatland.setLighting` APIs remain unchanged. No lighting entity, trait, store, selector, dirty range, or runtime type becomes public.                                                                                                                                                                                                    |
| Ownership audit        | A second light SoA cannot replace the per-frame projection: `Object3D.position`, `Color`, direction, and public scalar properties can mutate through existing Three/R3F paths. Copying those values into the GPU texture remains necessary; duplicating them adds coherence risk.                                                                                                     |
| Performance hypothesis | With the default 1,024-light capacity, the current tail loop performs roughly `2 × (1024 - count)` writes per frame even when the count is stable. Clearing only the previous live tail on count shrinkage makes sparse-light sync proportional to active lights plus removals.                                                                                                       |
| Required behavior      | One/dense lights, movement and scalar mutation, type/category changes, count growth/shrink/regrowth, disabled lights, exact stale-slot clearing, texture publication, nested worlds, terminal disposal, and first-error cleanup.                                                                                                                                                      |
| Required evidence      | Frozen Labs comparison at 1, 64, and 1,000 lights; exact packed-row tests; full lighting/shadow/package gates; Three/React lighting builds; size and declaration-boundary checks.                                                                                                                                                                                                     |
| Public attribution     | Any public guide or release note credits [Koota](https://github.com/pmndrs/koota)'s typed-trait/SoA design as the foundation that taught Flatland to separate object ownership from packed numeric projection, while recommending Koota for general-purpose application ECS.                                                                                                          |
| Branch                 | `feat/animated-sprite-playback-soa`                                                                                                                                                                                                                                                                                                                                                   |
| Pull request           | Pending                                                                                                                                                                                                                                                                                                                                                                               |
| Baseline               | Frozen fixture commit `44519c80`, captured from clean source `41f1481e`; p50: 1 light 1.61 µs, 64 lights 2.03 µs, 1,000 lights 13.52 µs. Baseline clock drift was 5.6%; every measurement was stable.                                                                                                                                                                                 |
| Commit                 | `45448114` (`perf(lighting): bound stale light slot clearing`)                                                                                                                                                                                                                                                                                                                        |
| Evidence               | Candidate p50: 1 light 426.38 ns (-73.6%), 64 lights 889.36 ns (-56.2%), 1,000 lights 13.64 µs (+0.9%, neutral); all `p < .001`, all measurements stable, conservative noise bands ±4%/±3%/±3%, candidate clock drift 10.6%. Package: 111 files / 1,422 tests, no type errors; type-aware lint; build/declaration boundary; Three/React lighting builds; full bundle 84.77 kB Brotli. |
| Result                 | Accepted. `LightStore` remains the one packed CPU/GPU projection. Stable sparse scenes no longer rewrite never-live capacity; only slots made stale by a count reduction have intensity and enabled lanes cleared. Dense-light cost is unchanged within the evidence threshold, and no public API or declaration changed.                                                             |

## ECS-007: Hierarchy refinement

| Field                  | Record                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Decision               | Keep Three.js `Object3D` identity and authored parentage authoritative. Accept a batch-local, frame-stamped projection that computes each shared parent path once rather than walking it once per sprite.                                                                                                                                                                                                                                                           |
| Numeric state          | Existing sprite-local matrix snapshots remain owner-local. The cache stores only frame stamps and aggregate changed/visible flags for unique source parents; it does not mirror transforms or create public hierarchy entities.                                                                                                                                                                                                                                     |
| Object state           | `Object3D` parents, matrices, visibility, clipping ancestors, sprite objects, batch meshes, and GPU resources remain object-owned.                                                                                                                                                                                                                                                                                                                                  |
| Public boundary        | No API change. Existing Three.js parenting, `SpriteGroup`, automatic orchestration, and React child composition remain the command surface.                                                                                                                                                                                                                                                                                                                         |
| Baseline               | Clean Labs result `hierarchy-path-baseline` at 16,384 sprites: direct static p50/average 6.50/6.70 ms; stable depth-three hierarchy 7.78/7.95 ms; one moving shared root 11.19/11.54 ms; 1,024 moving leaves 11.76 ms average and noisy. Baseline CPU clock drift was 7.6%, so the paired Labs statistical verdict controls acceptance.                                                                                                                             |
| Performance hypothesis | The stable hierarchy adds roughly 1.9 ms over direct roots even though its 1,024 shared leaf paths contain only 3,072 unique ancestors. Caching each unique parent path's changed/visible result once per frame should remove repeated ancestor walks and WeakMap reads without changing matrix composition, picking, dirty rows, or authored ownership.                                                                                                            |
| Required behavior      | Direct roots, depth-three shared parents, visibility at every level, root motion, leaf motion, reparenting, auto orchestration, hierarchy clipping, row reuse, hostile `updateMatrix()` callbacks, removal, and disposal.                                                                                                                                                                                                                                           |
| Required evidence      | Exact behavior tests; warm allocation check; same-fixture Labs comparison for direct/static/moving hierarchy cases; package size; Three/React hierarchy example builds; public declaration boundary.                                                                                                                                                                                                                                                                |
| Public attribution     | Any public explanation credits [Koota](https://github.com/pmndrs/koota) as the design foundation and continues to recommend it for general-purpose application/gameplay ECS.                                                                                                                                                                                                                                                                                        |
| Branch                 | `feat/animated-sprite-playback-soa`                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Pull request           | Pending                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Commit                 | `995b6805` (`perf(hierarchy): cache shared parent paths`)                                                                                                                                                                                                                                                                                                                                                                                                           |
| Evidence               | Clean Labs candidate `hierarchy-path-framestamp-candidate`: direct roots -5.1% p50, neutral inside +/-9%; stable depth-three hierarchy -4.0% p50 (`p < .001`, +/-3%); moving shared root -11.6% p50 (`p < .001`, +/-8%); independent-leaf motion noisy and excluded. Average measured heap deltas for the trusted hierarchy cases changed 52,901 -> 51,610 bytes and 351,983 -> 335,313 bytes. Candidate clock drift was 6.6%.                                      |
| Result                 | Accepted. Frame-stamped `WeakMap` records reuse each unique shared parent projection without the cleared-`Map` allocation regression. A package-private standard-method registration preserves the fast path without an import cycle; any custom or monkeypatched virtual `updateMatrix` disables caching for the remainder of the frame and retains exact per-sprite ordering. Three.js remains the sole hierarchy owner and no public API or declaration changes. |

## ECS-008: Camera-visible instance compaction

| Field                  | Record                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Decision               | Rejected. Keep the current contiguous SpriteBatch projection and do not add per-sprite camera compaction.                                                                                                                                                                                                                                                                                                                                               |
| Authoritative owner    | `Sprite2D` and the Three.js hierarchy retain transforms, visibility, camera layers, sorting, picking, and identity. The private world owns batch membership; any visible set is a camera-specific render projection only.                                                                                                                                                                                                                               |
| Existing projection    | `SpriteSpatialGrid` already tracks world-space picking bounds. `SpriteBatch` deliberately uses an infinite bounding sphere and a contiguous submitted prefix, so `SpriteGroup.frustumCulling` does not currently provide per-sprite visible-row compaction.                                                                                                                                                                                             |
| Tilemap boundary       | `TileLayer` already publishes finite-bound regional `InstancedMesh` chunks with `frustumCulled = true`; Three.js rejects offscreen map regions without a per-tile ECS projection. Regression `2b946044` pans a real camera between sparse chunks and proves the visible/intersection set changes exactly.                                                                                                                                               |
| Performance hypothesis | Large worlds with 5–20% camera occupancy can reduce upload/submission work by at least 15%; 100%-visible scenes must remain neutral. CPU transform work can fall only if a camera-correct boundary runs before the ECS schedule.                                                                                                                                                                                                                        |
| Required evidence      | Pinned Labs at 16,384/60,000 sprites and 5%/20%/100% occupancy; actual submitted-instance counts; multiple cameras; allocation; row/picking/sort parity; lifecycle; package/size/examples/live WebGPU.                                                                                                                                                                                                                                                  |
| Public boundary        | Existing `SpriteGroup.frustumCulling` remains the control surface unless a separate alpha API review proves another option necessary. No camera query, remap table, selector, entity, or typed storage becomes public.                                                                                                                                                                                                                                  |
| Public attribution     | Public material credits [Koota](https://github.com/pmndrs/koota) as the typed-query/SoA foundation and continues to recommend it for general-purpose application and gameplay ECS.                                                                                                                                                                                                                                                                      |
| Plan                   | `planning/superpowers/specs/2026-08-25-visible-instance-compaction.md`                                                                                                                                                                                                                                                                                                                                                                                  |
| Branch                 | `feat/ecs-visible-instance-compaction`                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Pull request           | Pending                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Evidence               | Clean Labs baseline `2026-08-25_06-04-10` at `f092418b`: p50 16,384 dense/20%/5% = 7.3508/7.2819/7.8347 ms; p50 60,000 dense/20%+motion/5%+motion = 30.4967/29.4537/28.5043 ms. Occupancy does not reduce the current schedule cost. Source inspection confirmed Three.js supplies a draw camera after `SpriteGroup.updateMatrixWorld()` runs the schedule, so per-draw candidate A cannot claim same-frame transform savings.                          |
| Rejected experiment    | Benchmark-only `0cd3b3d0` used the existing picking grid and copied matrix + packed rows into reusable visible buffers. Clean result `visible-row-prototype` changed p50 by +42.3%/+18.0%/+2.8% at 16,384 dense/20%/5% and +34.3%/+11.4%/+6.5% at 60,000; average measured heap rose in every case. Clock drift was 8.4%, so timing percentages are descriptive only, but the deterministic allocation failure and dense regression reject candidate A. |
| Candidate B review     | Rejected before code. Transparent sprites are globally `zIndex`-sorted inside one material batch. Spatial cells create separate draw calls whose object-level order cannot interleave overlapping sprite depths exactly, violating alpha/sort parity.                                                                                                                                                                                                   |
| Candidate C evidence   | Benchmark-only `b520c534` writes visible source slots without row copies. Clean result `visible-index-prototype`: p50 +20.8%/+12.9%/+2.8% at 16,384 dense/20%/5%, and +13.7%/+6.2%/+5.4% at 60,000. Clock drift was 8.3%; paired values are descriptive. Sparse 60,000 preparation costs 1.54–1.74 ms and measured heap still rises.                                                                                                                    |
| Candidate C result     | Rejected and removed by `ad861d73`. Public TSL indirection primitives exist, but the prototype fails the prerequisite CPU/allocation gate before shader integration: dense cases regress, sparse 60,000-sprite preparation adds 1.54–1.74 ms, and measured heap rises.                                                                                                                                                                                  |
| Result                 | Closed with no production runtime residue. Candidates A and C failed CPU/allocation gates; candidate B failed transparent-sort correctness. Reopen only for a new workload or camera-to-slot primitive that proves allocation-free preparation and a neutral dense path before headed GPU work.                                                                                                                                                         |

## ECS-009: Shadow-occluder projection

| Field                  | Record                                                                                                                                                                                                                                    |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Decision               | Active, benchmark-first. Maintain stable managed occluder membership, compare cached swap lists with a persistent shadow-view scene, and retain exact foreign-mesh behavior.                                                              |
| Authoritative owner    | Sprite batches, tile chunks, materials, textures, geometry, camera layers, and Three.js scene identity remain object-owned. The projection owns only derived membership, revisions, occlusion materials, and optional shadow-view meshes. |
| Existing projection    | `shadowPipelineSystem` already gates clean frames by camera/occluder revisions. Dirty regeneration still traverses the complete scene, discovers eligible meshes, swaps materials, renders, and restores them.                            |
| Performance hypothesis | Stable membership can remove discovery and possibly swap/restore cost when 256–2,048 managed meshes/chunks regenerate shadows. The 32-source case must remain neutral, and full WebGPU pass time controls acceptance.                     |
| Required evidence      | Pinned Labs dirty-pass preparation, headed WebGPU timing/output parity, 1/8/64 material keys, foreign custom meshes, topology churn, nested worlds, lighting replacement, first-error disposal, package/size/examples.                    |
| Public boundary        | Existing `castsShadow`, `receiveShadows`, `LightEffect.needsShadows`, and camera-layer APIs remain the command surface. Projection membership and lifecycle hooks stay package-private and declaration-unreachable.                       |
| Public attribution     | Public material credits [Koota](https://github.com/pmndrs/koota) as the system/query foundation that enabled stable renderer projections and continues to recommend it for general-purpose application and gameplay ECS.                  |
| Plan                   | `planning/superpowers/specs/2026-08-25-shadow-occluder-projection.md`                                                                                                                                                                     |
| Branch                 | `feat/ecs-shadow-occluder-projection`                                                                                                                                                                                                     |
| Pull request           | Pending                                                                                                                                                                                                                                   |
| Result                 | Pending evidence.                                                                                                                                                                                                                         |

## ECS-010: Skia retained display list

| Field                  | Record                                                                                                                                                                                                                                                                                     |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Decision               | Queued. Open a benchmark-only GitHub issue and do not integrate a display list until complete WebGPU redraw evidence passes.                                                                                                                                                               |
| Authoritative owner    | Public `SkiaNode`/`SkiaGroup`/`Object3D` topology, properties, and Skia resources remain authoritative. A candidate display list would be a package-private command projection with numeric lanes and object-reference tables.                                                             |
| Performance hypothesis | Repeated dirty redraws containing 10,000–50,000 mostly homogeneous shapes may benefit from dense traversal. UI-sized trees and resource-heavy draws may remain Skia/GPU-bound and must not regress.                                                                                        |
| Required evidence      | Benchmark-only PR at 1,000/10,000/50,000 nodes; compilation, 1% mutation, JavaScript traversal, complete WebGPU redraw, retained memory, pixel parity, nested groups, custom nodes, Three/R3F, and disposal.                                                                               |
| Package boundary       | The prototype is local to `@three-flatland/skia`. It cannot import Flatland's unpublished renderer ECS or require publishing that runtime. Only small type-erased helpers without world/entity/trait/query/system APIs may be shared. Otherwise the Skia projection remains purpose-built. |
| Public boundary        | No display-list, command-buffer, node ID, compiler, ECS runtime, or ECS handle becomes public. Existing Skia classes and setters remain the command surface.                                                                                                                               |
| Public attribution     | Public material credits [Koota](https://github.com/pmndrs/koota) as the identity-plus-dense-iteration foundation and continues to recommend it for general-purpose application and gameplay ECS.                                                                                           |
| Plan                   | `planning/superpowers/specs/2026-08-25-skia-display-list.md`                                                                                                                                                                                                                               |
| GitHub issue           | [#235](https://github.com/thejustinwalsh/three-flatland/issues/235)                                                                                                                                                                                                                        |
| Pull request           | Benchmark only, pending issue acceptance                                                                                                                                                                                                                                                   |
| Result                 | No implementation authorized.                                                                                                                                                                                                                                                              |
