---
"@three-flatland/devtools": major
---

Renamed from `@three-flatland/tweakpane` to `@three-flatland/devtools` to reflect the package's growing scope (tweakpane UI plugin + stats monitor + buffer inspection). The old package name is deprecated; update imports to `@three-flatland/devtools` and `@three-flatland/devtools/react`. The sub-export paths and runtime API are unchanged.
